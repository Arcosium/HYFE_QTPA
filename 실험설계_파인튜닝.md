# 파인튜닝 실험 설계 (사전등록안 v1.1 · 순차안)

작성 2026-09-07 · 실험 기간 9/14~10/4 · 동결 10/5 · 중간발표 10/8
정본 데이터 CryptoBars 1분봉 (약 8.3억 행, 1,057종목, 상폐 포함, 2023-01-01부터 수집 계속 중. 정본 스냅샷은 P0 종료일 9/13에 고정하고 그 뒤 수집분은 P7 페이퍼 트레이딩에만 쓴다)

v1(완전요인 1,300런·H100)은 예산 초과로 폐기. v1.1은 신호가 있을 확률이 높은 축부터 순서대로 열고, 각 단계의 승자 위에서 다음 축을 연다.

## 0. 한 줄 요약

9단계 순차. 약 300런, A40 약 240시간, pod 2대로 실제 5~6일, 예산 약 $150~200(A40 $0.6~0.8/h). H100은 쓰지 않는다.
선택은 검증 폴드에서만 하고, 판정은 손대지 않은 홀드아웃(2026-03~08 + 미학습 심볼)에 마지막 한 번만 한다.

## 1. 유의성을 지키는 규칙 (실험 전에 고정)

1. 사전등록. 이 문서의 단계 순서·선택 규칙·판정 기준을 실험 시작 전에 고정한다. 바꾸면 v1.2로 기록하고 이유를 남긴다.
2. 선택과 판정을 분리한다. 선택은 검증 폴드 PR-AUC로만 한다. 판정은 최종 홀드아웃(시간 2026-03~08, 종목 미학습 20%)에서 S8 후보 3개에 한 번만 한다. 홀드아웃을 두 번 보면 그 결과는 무효다.
3. 전체 결과를 남긴다. 이긴 설정만이 아니라 모든 런의 표(리더보드 csv)를 보고서에 붙인다. 순차안은 런이 300개라 표 하나에 다 들어간다.
4. 기준선 3종을 모든 단계에 동반한다. B0 무정보(클래스 비율), B1 규칙(직전 σ 돌파 시 같은 방향), B2 GBM(같은 창의 표 피처).
5. 플라시보. 후보마다 라벨을 섞어 다시 학습시키고 PR-AUC가 클래스 비율 수준으로 무너지는지 본다. 안 무너지면 누수다.
6. 누수 차단. 시간은 퍼지+엠바고(W+H 봉)로 자르고, 같은 base의 다거래소 중복은 거래소 1개만 남긴다. 정규화 통계(σ, 거래량 평균)는 창 안 과거값만 쓴다.
7. 불확실성. 후보는 시드 5개 × 폴드 4개. 신뢰구간은 종목×월 블록 부트스트랩 95%. 최종 비교는 GBM 대비 폴드 짝지은 차이에 Holm 보정.
8. 순차안의 약점은 축 사이 상호작용(예: 거래량이 1분봉에서만 도움)을 놓칠 수 있다는 것이다. S6에서 효과가 컸던 두 축의 2×2로 한 번 확인한다.

## 2. 데이터 준비 (GB10에서 전처리만, 학습은 전부 Runpod)

| 항목 | 내용 |
|------|------|
| 정본 | base당 거래소 1개(우선순위 binance > okx > bybit > 기타). bybit 자체집계 145종·토큰화 주식·원자재 제외. 상폐 종목은 남긴다 |
| 리샘플 | 1m → 5m·15m·1h·4h·1d. UTC 경계 정렬, OHLC 표준 집계, volume 합. quote_volume 없는 거래소(hyperliquid·paradex)는 close×volume으로 대체하고 표시 |
| 창 표본 | 창 시작 간격 = H/2 봉. 학습 집합은 양성 전부 + 음성 다운샘플로 해상도당 최대 200만(S8 후보만 500만) |
| 시간 구간 | **IS(학습) / OS(검증, 선택 전용) / ROS(Real OS, 판정 전용)** 세 구간(사장 지시 9/7). 선택은 OS 로만, ROS 는 판정에만 쓴다 |
| 롤링 폴드 | 4개 walk-forward, 3개월씩 민다. F1 IS ~2024-02/OS 2024-03~05/ROS 2024-06~08 · F2 ~2024-08/09~11/2024-12~2025-02 · F3 ~2025-02/03~05/06~08 · F4 ~2025-08/09~11/2025-12~2026-02. IS 는 확장형(2023-01부터)과 고정 길이 롤링(`--train_months`) 둘 다 낸다 |
| 최종 홀드아웃 | 2026-03~2026-08. ROS 바깥의 마지막 판정. S8에서 IS ~2026-02로 한 번만 평가 |
| 종목 분할 | 심볼 이름 해시로 20%를 미학습 고정. 모든 종목군 실험이 같은 미학습 집합에서 평가받는다 |
| 스테이징 | 정본 parquet(약 10GB)를 Runpod 네트워크 볼륨에 1회 rclone. 이미지는 GPU 즉석 렌더, 디스크에 저장하지 않는다 |

해상도별 규모(base 약 700개 기준 추정, P0 감사에서 확정):

| 해상도 | 봉 수 | 창 후보(간격 H/2) | 학습 집합 상한 |
|--------|-------|------------------|----------------|
| 1m | 12.9억 | 4,300만 | 200만 |
| 5m | 2.6억 | 2,100만 | 200만 |
| 15m | 8,600만 | 1,400만 | 200만 |
| 1h | 2,150만 | 700만 | 200만 |
| 4h | 540만 | 270만 | 200만 |
| 1d | 90만 | 85만 | 85만 |

일봉은 표본이 가장 얇다. 일봉 결과가 약하면 표본 부족인지 정보 부족인지 구분해야 하므로, S0에서 1h 결과를 일봉 표본 수로 줄인 대조를 같이 돌린다.

## 3. 실험 축과 수준

| 축 | 기호 | 수준 | 기본값 |
|----|------|------|--------|
| 해상도 | R | 1m · 5m · 15m · 1h · 4h · 1d | S0이 정한다 |
| 창 길이(봉) | W | 60 · 120 · 240 (480은 JSON만) | S0이 정한다 |
| 지평 | H | W/12 · W/4 · W/2 (봉 단위) | S0이 정한다 |
| 거래량 | V | 있음(하단 패널) · 없음 | 있음 |
| 라벨 | L | L1 kσ 3클래스 k=2 · L2 k=1.5 · L3 k=3 · L4 고정 ±(해상도별 중앙 \|수익률\|×3) · L5 이진 상승/하락(JKX식) | L1 |
| 표현·모델 | M | 아래 §5 | I1 |
| 종목군 | G | 아래 §6 | G3 |
| 렌더 방식 | D | D1 OHLC 바(JKX) · D2 캔들스틱 · D3 라인+MA20 · D4 2채널(가격/거래량 분리) | D1 |

해상도 × 창 길이가 뜻하는 기간:

| | W=60 | W=120 | W=240 | W=480 |
|--|--|--|--|--|
| 1m | 1시간 | 2시간 | 4시간 | 8시간 |
| 5m | 5시간 | 10시간 | 20시간 | 40시간 |
| 15m | 15시간 | 30시간 | 2.5일 | 5일 |
| 1h | 2.5일 | 5일 | 10일 | 20일 |
| 4h | 10일 | 20일 | 40일 | 80일 |
| 1d | 60일 | 120일 | 240일 | 480일 |

## 4. 순차 설계 — 유의할 확률 높은 것부터

각 단계는 앞 단계의 승자 설정 위에서 한 축만 연다. 런당 시간은 A40 기준(I1 학습 200만 표본 약 1시간, GBM 5분, I2 약 3시간).

| 단계 | 기간 | 무엇을 | 왜 이 순서인가 (사전 기대) | 런 | A40 시간 | 다음 단계로 가는 조건 |
|------|------|--------|---------------------------|----|----------|----------------------|
| S0 GBM 지도 | 9/14~15 | J1 GBM(밀도 피처 40개) × R 6 × W 3 × H 3 = 54 설정 × 4 폴드 + 일봉 표본 대조 | 거래량 급증 뒤 변동성 확대는 잘 알려진 현상이라 여기서 이미 무정보 대비 유의해야 한다. 어느 해상도·지평에 예측 가능성이 있는지 지도가 나오고, 5분씩이라 거의 공짜 | 220 | 18h | 상위 (R,W,H) 2개가 B0·B1을 95% CI 밖에서 이길 것. 못 이기면 라벨 재설계 |
| S1 이미지 핵심 검정 | 9/16~18 | I1 소형 CNN × 상위 2 설정 × 4 폴드 + 플라시보 2 | 연구의 핵심 주장 "차트 이미지가 표 피처보다 낫다"를 가장 싼 모델로 먼저 본다. 사전 예상은 5m·15m에 1일치 창, H 1~4시간 | 10 | 10h | P3 게이트 3종(GBM 초과·셔플 붕괴·폴드 간 방향 일치). 탈락 시 §9 |
| S2 거래량·라벨·지평 | 9/19~22 | 승자 위에서 V 2 × L 3(L1·L2·L5) × H 2 = 12 설정(승자 중복 제외 11) × 4 폴드 | 사장이 정한 세 질문 중 두 개. 거래량은 도움될 것, kσ가 고정%·이진보다 나을 것으로 예상 | 44 | 44h | 검증 PR-AUC 상위 1 설정 |
| S3 표현 | 9/23~26 (추석, pod만 돈다) | I2 사전학습 ResNet-18 · J2 PatchTST · 렌더 D2~D4, 각 4 폴드 | "이미지를 그대로 vs 구조화 시퀀스" 질문. 사전학습이 이기면 I3 ConvNeXt 추가 4런 | 20(+4) | 28h(+12) | 표현·렌더 승자 1 |
| S4 종목군 | 9/27~29 | G1~G5 + 상폐 제외 2 = 7 × 4 폴드. 평가는 항상 같은 미학습 20% | 종목 폭이 일반화를 사는지, 잡음 종목이 해치는지 | 28 | 28h | 미학습 PR-AUC 승자 1 |
| S5 융합 | 9/30 | F1 이미지 + 밀도 피처 late fusion, 4 폴드 | 연구 제목의 두 축을 합친다. 완만한 개선 예상 | 4 | 4h | |
| S6 상호작용 | 10/1 | S2~S4에서 효과가 컸던 두 축의 2×2, 4 폴드 | 순차안이 놓친 조합 확인 | 16 | 16h | |
| S7 JSON→LLM | 9/23~ (두 번째 pod, 병행) | J3 Qwen3.5-8B LoRA, S2 승자 창을 JSON 텍스트로. F4 폴드 1런 | "이미지 vs JSON" 비교의 마지막 팔. 수치 시퀀스에서 LLM은 J2에 질 것으로 예상하지만 논문에 필요한 대조 | 1(+1) | 30h(+15 4B) | |
| S8 확인·홀드아웃 | 10/2~10/4 | 리더보드 상위 3(모델 종류 2개 이상) × 시드 5 × 4 폴드, 500만 표본. 그 다음 학습 ~2026-02로 홀드아웃 1회 | 최종 판정 | 63 | 63h | 승자 규칙: 미학습 PR-AUC 우선, 동률이면 단순한 모델. 10/5 동결 |
| 합계 | | | | 약 300 | 약 240h | |

S0~S6·S8은 pod 1이 순서대로, S7은 pod 2가 따로 돈다. pod 1 기준 실제 약 210시간 = 9일이지만 S3·S4는 폴드 4개를 pod를 하나 더 띄워 나누면 5~6일로 줄어든다.

## 5. 표현·모델 (축 M)

같은 창을 세 가지로 표현한다. 셋 다 같은 창·같은 라벨·같은 폴드를 쓴다.

| 기호 | 표현 | 모델 | 비고 |
|------|------|------|------|
| I1 | 그레이스케일 이미지, 봉당 3px, 높이 96(거래량 패널 포함 시 +24) | JKX형 소형 CNN(3블록, 약 70만 파라미터) 처음부터 학습 | 기본. 가장 싸다 |
| I2 | 같은 이미지를 224×224로 리사이즈 | ImageNet 사전학습 ResNet-18 전층 파인튜닝 | S3 |
| I3 | 같음 | ConvNeXt-Tiny 파인튜닝 | S3에서 I2가 I1을 이길 때만 |
| J1 | 창을 표 피처로 압축(수익률 통계·RSI·MA비율·§5.1 밀도 피처 약 40개) | LightGBM | 기준선 B2 겸용, S0 지도 |
| J2 | 정규화 시퀀스 W×5(o,h,l,c,v를 첫 종가·평균 거래량으로 나눔) | PatchTST(소형 트랜스포머) | 이미지 없이 원자료를 그대로 본다 |
| J3 | 위 시퀀스를 JSON 텍스트로 직렬화(소수 4자리, 약 1,200토큰) | Qwen3.5-8B LoRA, 답은 클래스 토큰 하나 | S7, pod 2 |
| F1 | I-best 특징맵 + 밀도 피처 결합 후 분류 헤드 | late fusion | S5 |

### 5.1 거래 밀도 피처 (J1·F1 공통, 창 안 과거값만)

상위 5봉 거래량 점유율, 거래량 HHI, 최근 10봉 거래량/창 평균, 거래량이 중앙값 3배를 넘는 봉 수, 달러 거래량 z(창 대비 30일), 봉 범위/거래량(아미후드형), 무거래 봉 비율, 상승봉·하락봉 거래량 비.

## 6. 종목군 (축 G)

| 기호 | 정의 | 목적 |
|------|------|------|
| G1 | 메이저 10 고정(BTC·ETH·SOL·BNB·XRP·DOGE·ADA·AVAX·LINK·TON, 사장 확정 필요) | 유동성 최고, 표본 최소 |
| G2 | 유동성 상위 50. 학습 구간의 평균 달러 거래량으로 폴드마다 재산정(미래 정보 금지) | 실전 매매 가능 종목 |
| G3 | 유동성 상위 200 | 기본값 |
| G4 | 커버리지 80% 이상 전체(상폐 포함) | 생존편향 없는 최대 표본 |
| G5 | 변동성 3분위 층화 150(저·중·고 50씩) | 변동성 편향 통제 |

상폐 포함/제외는 G4·G5에서만 의미가 있다. 평가는 종목군과 무관하게 같은 미학습 20%(G4에서 추출)에서 하고, 종목군 안의 시험 폴드 성적도 따로 적는다.

## 7. 판정 지표

- 주지표: PR-AUC(급등·급락 두 클래스 평균). 부지표: ROC-AUC, 상위 1% 신호 정밀도, 브라이어.
- 경제 지표(P6 전 예비): 신호 십분위 롱숏 스프레드(수수료 0.05%+슬리피지 0.05% 반영), 이벤트 적중 후 H봉 평균 수익.
- 단계별 효과 표: 단계마다 수준별 평균 PR-AUC ± 95% CI. 이것이 중간발표의 핵심 그림이다.
- 유의성 문장 형식: "I1(15m·96·거래량·kσ2)은 미학습 심볼 홀드아웃에서 PR-AUC 0.xx [0.xx, 0.xx], GBM 대비 +0.0x (Holm 보정 p=0.0x, 폴드 4·시드 5)". 이 형식을 채우지 못하면 유의하다고 쓰지 않는다.

## 8. GPU·비용

| 모델 | 필요 메모리 | 알맞은 GPU | 이유 |
|------|------------|-----------|------|
| I1 소형 CNN·J2 PatchTST | 수 GB | A40 48GB 또는 RTX 4090 24GB | 연산이 작아 H100은 낭비. 병목은 즉석 렌더·데이터 로딩 |
| J1 LightGBM | CPU | pod의 CPU | GPU 불필요 |
| I2 ResNet-18 224px | 약 10GB | A40 | |
| J3 Qwen3.5-8B LoRA | bf16 가중치 16GB + LoRA 옵티마이저 1GB 미만 + 활성값(1,200토큰·배치 4·체크포인팅) 약 8GB = 약 25GB | A40 48GB에 여유. QLoRA 4bit면 4090 24GB도 가능 | 전 파라미터 파인튜닝은 130GB 이상이라 애초에 계획에 없다 |

- A40은 H100의 1/3 속도. J3 1런이 H100 10시간 → A40 30시간이지만 비용은 $30 → $21로 오히려 싸다. 시간이 급한 팔이 아니므로 A40.
- pod 2대(A40, 네트워크 볼륨 공유). 총 약 240시간 × $0.6~0.8 = $150~200. 상한 $250 제안.
- 오케스트레이션은 최소로: `runs.csv`(단계별 격자) + `worker.py`(자기 몫만 실행, 결과는 볼륨의 `results/<run_id>.json`) + GB10의 `collect.py`(rclone 동기화 후 리더보드·단계 효과 표). Runpod은 REST만 쓰고 유휴 pod는 즉시 종료한다.
- GB10에서는 학습을 돌리지 않는다(통합메모리 정지 사고). 정본·리샘플·라벨·피처 계산까지만.

## 9. 일정과 물러설 규칙

| 주 | 팀장 | 비고 |
|----|------|------|
| 9/7~9/13 | P0 정본·리샘플·라벨 엔진·렌더러·JSON 변환기·밀도 피처·평가 하네스·Runpod 스테이징 | 이 문서 사전등록 |
| 9/14~9/18 | S0 GBM 지도 → S1 이미지 핵심 검정(게이트) | 9/21(월) 결과 공유 |
| 9/19~9/29 | S2 → S3 → S4, pod 2는 S7 | 추석 9/24~26은 pod만 돌고 사람은 쉰다 |
| 9/30~10/1 | S5·S6 | |
| 10/2~10/4 | S8 확인·홀드아웃 판정 | |
| 10/5~10/8 | 동결·중간발표 | |

- S0에서 GBM이 무정보를 못 이기면: 라벨 재설계에 3일(k·H 재탐색), S1부터 다시.
- S1 게이트 탈락: 라벨 재설계 1주(9/19~25), S2는 V와 L만으로 축소.
- 일정 지연: S6과 S3의 렌더 D2~D4를 뺀다. S7은 결과가 늦어도 중간발표엔 "진행 중"으로 넣는다.
- 무엇을 버리든 S8 홀드아웃 한 번 판정 원칙은 유지한다.

## 실행 기록

### v1.2 (9/7) — S0 를 로컬 표본으로 앞당김, 선택 규칙 보정

사장 제안으로 전수 데이터 전에 표본(유동성 상위 60종목, 1m·5m 는 2025-01부터, 15m·1h 는 2024-01부터, 4h·1d 는 2023-01부터)으로 S0 GBM 지도를 GB10 CPU 에서 먼저 돌렸다(LightGBM, 학습 30만 행 상한, 분할 2개). 결과 `work/pilot_gbm.csv`.

- 모든 해상도에서 GBM 이 무정보(유병률)를 1.5~2.6배 이긴다. S0 게이트 통과.
- 해상도 평균 lift: 15m 1.98 · 5m 1.90 · 1h 1.88 · 1m 1.69 · 4h 1.63. 1d 는 표본 부족으로 대부분 학습 불가(60종목으로는 창 240봉이 안 나온다).
- 짧은 지평(H=W/12)이 긴 지평보다 일관되게 낫다. 변동성 군집이 주된 신호라는 뜻이다.
- 규칙 보정: 표본 학습 행이 5만 미만인 설정(1h·4h 의 긴 창)은 검증·시험 성적이 크게 어긋나(예: 1h·120·60 검증 2.58 vs 시험 1.68) 선택에서 뺀다.
- S1 선택: 검증 lift 상위 3개 — **1h·60·5 (2.26), 15m·120·10 (2.22), 15m·60·5 (2.15)**. 셋 다 검증·시험·미학습 성적이 일치한다. 사전등록은 2개였지만 런당 1시간이라 3개로 늘렸다.
- S1 은 Runpod A40(secure, $0.35/h + 디스크) 에서 유동성 상위 200종목·2023-01부터 전 구간으로, GBM 기준선을 같은 데이터·분할로 다시 낸다.

### v1.3 (9/7 밤) — 병렬 함대·학습률 축 추가·로컬 CPU 병행

- 사장 지시로 A40 pod 를 9대로 늘려 S1 과 S2 를 동시에 돌린다(pod 당 $0.49/h). 1h 설정은 첫 pod, 15m 설정 2개는 pod 4대(S1), S2 변형 10개는 pod 4대. 데이터는 pod 간 복사.
- S1 첫 결과(1h·60·5, lr 1e-3): CNN 검증 lift 1.87 < GBM 2.37. 1에폭에서 정점 뒤 하락하는 과적합. JKX 원논문 학습률(1e-5)보다 100배 커서 **학습률을 S1 의 축으로 추가**: 15m 두 설정을 lr 1e-3 과 1e-4 로 각각 돌리고 검증으로 고른다. S2 는 1e-4 로 먼저 돌린다(변형 간 상대 비교라 유효).
- 로컬 GB10 은 CPU 만 쓴다(사장 허용, 학습 제외): 전수(상위 200·2023-01~) GBM 지도 54설정, S4 종목군 GBM 판, 나머지 540종목 봉 캐시.
- 함대 종료는 `work/fleet.json` 의 id 이면서 이름이 `hyfe-` 인 pod 만. 다른 서비스 pod 보호.

### 전수 GBM 지도 결과 (9/7, 상위 200종목·2023-01~, 종목당 창 2만 상한, 폴드 2) — `work/results/gbm_map_top200*.csv`

발생(급등·급락) 예측 lift 상위: 5m·240·20 **2.31**, 15m·120·10 2.19, 1h·60·5 2.16, 15m·240·20 2.15, 5m·120·10 2.15, 15m·60·5 2.14. 해상도 평균은 5m 1.98 ≈ 15m 1.94 > 1h 1.77 > 1m 1.69 > 4h 1.65 > 1d 1.59. 표본 지도와 순위가 같아 S1 선택(1h·60·5, 15m·120·10, 15m·60·5)은 유지하되 5m·240·20 을 S3 단계에서 추가 확인한다.

방향(급등 확률−급락 확률 상·하위 10% 선행수익 차, σ 단위) 은 **지평이 길어야 나타난다**: H=5~10봉에서는 0.0~0.1σ, 15m·240·60 0.25/0.32σ(ROS/미학습), 15m·120·60 0.27/0.31σ, 1h·60·15 0.28/0.28σ. 1m 은 지평과 무관하게 0. 발생 예측과 방향 예측의 최적 지평이 다르므로 P6 전략에서 S4 위험 오버레이(짧은 지평 발생 예측)와 S2 방향 매매(긴 지평)를 분리한다.

### v1.4 (9/7 오후) — 게이트 결과와 방향 전환

- S1 게이트 ①(CNN > GBM) 기각. 15m 두 설정·1h 설정·학습률 2종·거래량 유무·지평 3종·백본 3종(소형 CNN·ResNet-18·시퀀스) 어디서도 이미지가 피처 GBM 을 넘지 못했다(예: 15m·120·10 OS lift GBM 2.45 vs CNN 2.05, 융합 2.21, 시퀀스 2.11). ②플라시보 붕괴·③분할 일치는 통과.
- 사전등록의 처방(라벨 재설계)은 적용하지 않는다. 같은 라벨로 GBM 이 4폴드에서 안정적으로 맞히므로 라벨은 문제가 아니고, 문제는 표현이다. 대신 이미지의 순기여를 스태킹(표본 밖 CNN 점수를 GBM 피처에 추가, 짝지은 부트스트랩)으로 판정한다.
- 경제성: 방향 신호는 긴 지평(15~60시간)에서만 비용을 넘고, 공매도 쪽에 몰려 있다. 롤링 4폴드 전부 양수(실험일지 참조). 머신은 GBM 기반 급락 공매도 + 발생 모델 위험 오버레이로 간다. 이미지 CNN 은 보조 피처 후보.
- 평가 구간 명칭을 IS/OS/ROS 로 통일(사장 지시), 롤링 4폴드는 `--splits 0,1,2,3`.

### v1.5 (9/7 밤) — 본 학습 파라미터 **확정** (앙상블 4폴드 확인 완료: 판단 시각 정렬 3모델 평균이 4폴드 전부 무작위 초과, 단일 모델의 손실 폴드 없음)

| 항목 | 확정 | 근거 |
|------|------|------|
| 해상도 | 15m (보조 1h) | 전수 지도 5m ≈ 15m > 1h > 1m; 1m 은 방향 0, 5m 은 15m 과 동급이나 비용 4배 |
| 발생(급등·급락) 모델 | 15m·120·10 (창 30시간, 지평 2.5시간), kσ k=2 | 4폴드 ROS lift 2.0~2.5, 급락 정밀도@5% 10~14% |
| 방향(매매) 모델 | 15시간 지평 3종: 15m·240·60, 15m·120·60, 1h·60·15 → 앙상블(시간 격자) | 짧은 지평은 왕복 20bp 에 먹힘, 15h 는 4폴드 중 3폴드 비용 후 양수 |
| 라벨 | kσ, k=2 (k=1.5·3·고정%·이진은 S2 결과로 비교 중) | GBM 이 안정, 고정%·이진은 공통 잣대(spread_z)에서 확인 뒤 |
| 모델 | GBM(밀도 피처 27 + 시각·요일) 주력, J2 시퀀스 보조, CNN 은 스태킹 순기여 확인 뒤 보조 피처 | GBM ≥ F1 ≈ J2 ≥ CNN > ResNet |
| 종목군 | 전수 740(상폐 포함), 매매 후보는 폴드/시점 직전 12개월 거래대금 상위 200 | S4: 넓을수록 미학습 일반화 ↑(방향 0.16→0.35) |
| 매매 | 공매도 편향(점수 하위 10%), 다음 봉 시가 진입, 15h 보유, K=20 동시 보유, 편도 0.1%+펀딩 | 롱은 유의하지 않음, 공매도만 비용 후 양수 |
| 학습 데이터 | IS 확장형(2023-01~), 학습 행 상한 300만(GBM)·500만(CNN·J2) | "전수로 많이" — 표본 축소 없이 |
| 평가 | IS/OS/ROS 롤링 4폴드 + 최종 홀드아웃 2026-03~08 한 번, 성적표(정밀도·재현율@1/5/10%) + 자본 제약 시뮬 + 무작위 대조 | 사장 지시 |

## 보고서·발표 서사 (사장 확정 9/7)

1. 문제 제기 — JKX·중국 A주 논문은 창 길이(5/20/60일)·지평·해상도·라벨을 주식 시장에 맞춰 임의로 정했다. 24시간 크립토에는 그대로 못 쓴다.
2. 방법 — 축 8개(해상도·창·지평·거래량·라벨·표현·종목군·렌더)를 정의하고, 모든 조합을 전수 학습하는 대신 **표본 선별 실험**(상위 60종목 GBM 지도 → 순차로 한 축씩, S0~S4)으로 후보를 좁혔다. 선택은 OS, 판정은 ROS, 플라시보·무작위 대조·짝지은 부트스트랩.
3. 발견 — 발생 예측은 짧은 지평·15m, 방향은 긴 지평(15h)에서만 살아남고 공매도 쪽에 몰린다. 가장 센 신호는 시각·요일·거래 밀도. 흑백 차트 CNN 은 정보량을 맞춘 피처와 동급이지만 맥락이 없어 전체 피처 GBM 에 진다.
4. 제안 — 차트에 **밀도·시간 채널을 더한 5채널 이미지**(기존 논문과의 차별점). ← 결과 칸: 갈래 A(피처 수준 도달·초과 → 5채널 CNN 주력) / 갈래 B(여전히 아래 → GBM+시퀀스 주력, 이미지는 비교 결과). 결과가 나오면 한 갈래만 남긴다.
5. 머신 — 전수 740종목 학습, 롤링 4폴드 + 최종 홀드아웃 2026-03~08, 급락 공매도 앙상블 + 발생 경보. 페이퍼 트레이딩 실측으로 마무리.

## 논문 3편 대조와 최종 방향 (9/7 밤, 본문 확인 — 중국 논문은 초록만)

| 항목 | JKX(JF 2023) | 중국 A주(SSRN 5136032) | arXiv 2605.00875 | 우리 |
|------|------|------|------|------|
| 데이터 | CRSP 일봉 1993~2019 | A주 일봉 | 야후 일봉 BTC·ETH, 500표본/종목 | 크립토 perp 1m 3.6년·740종목·상폐 포함 → 15m·1h |
| 창 | 5·20·60일 관행(탐색 없음) | 미상 | 14~90일 비교, 30일 | 표본 선별 실험 → 15m·120/240봉 |
| 라벨·지평 | 5·20·60일 수익률 부호 | H-L 십분위 | 7일·2% 고정, 이진 | kσ 사건 3클래스, 발생 2.5h / 방향 15h |
| 채널 | 흑백 1채널(OHLC·MA·거래량) | 2채널, 거래량·과거수익·MA 추가 시 개선 | 캔들 RGB, 거래량 넣으면 악화 | 5채널(차트·밀도·30일 거래량·시각·요일) |
| 모델 | 소형 CNN 5×3·2×1풀링, lr 1e-5, 5앙상블(2×2풀링·ReLU·레이어 축소는 악화) | CNN | 단순 CNN > ResNet > ViT | 소형 CNN 원형(i1f 폐기와 일치), ResNet 탈락 |
| 표 피처 대조 | 없음 | 없음 | 없음 | 정보량 맞춘 GBM·전체 GBM·시퀀스 |
| 검증 | 8년 학습 1회 → 19년 시험 | 미상 | 70/15/15, 종목 홀드아웃 없음 | IS/OS/ROS 롤링 4폴드·미학습 종목·홀드아웃·플라시보·무작위 대조 |
| 비용 | 없음 | 없음 | 없음 | 수수료·슬리피지·펀딩·자본 제약 |
| 장중·시각·밀도 | 없음 | 없음 | 없음 | 핵심 신호 |

**최종 방향 확정**: 머신 = 구조화 수치(GBM 발생 경보 + 15h 방향 앙상블, J2 후보). 이미지는 가설.
- H1 채널 가설: 5채널 CNN OS PR-AUC > 흑백 CNN, 4폴드 중 3폴드 이상.
- H2 도달 가설: 5채널 CNN vs 전체 GBM 짝지은 ΔAP 95% CI 가 3폴드 이상에서 0 포함 또는 위.
- H3 순기여 가설: 스태킹 ΔAP(GBM+CNN − GBM) 95% CI 가 3폴드 이상에서 0 위.
채택 시 5채널 CNN 을 앙상블에 편입, 기각 시 비교 결과로 보고. 기준은 결과 전에 고정(이 항목).

### v1.6 (9/7 심야) — S8 최종 홀드아웃 결과와 머신 재정의

- **홀드아웃(2026-03~08, 한 번) 결과**: 발생 모델 lift 1.96(미학습 2.03), 급락 경보 상위 5% 의 실제 급락률 6.2%(기저 2.4%) — 유지. 방향 앙상블 공매도 ×0.56, 거래당 −35bp(무작위 −19bp), 방향 spread_z 3모델 전부 음수 — **기각**.
- 원인: 롤링 4폴드 ROS 가 전부 알트 약세장(−32~−49%/분기)이라 방향 신호의 이익이 시장 하락 타이밍·체제였고, 횡보장 홀드아웃에서 뒤집혔다. 헤지·롱숏·문턱 5%·비용 절반·BTC 추세 필터 어느 것도 못 살림(사후 2차 평가로 표시). 회전율(하루 32건)이 비용 15~30%/분기를 만드는 구조적 문제도 확인.
- **머신 재정의**: ① 주력 = **급등·급락 발생 경보 머신**(15m·120·10 GBM, 상위 5% 경보, 시간당 갱신, 실시간 경보 정밀도 추적) ② 방향 공매도는 "약세장 한정, 실전 미채택" 으로 보고서에만 ③ 페이퍼 트레이딩은 경보 검증(표시 창의 실제 급락률·절대변동 추적)으로 대체. 경보의 실전 용도 = 위험 오버레이(표시 종목 노출 축소·레버리지 하향·스탑 확대).
- 검증 교훈(보고서 명시): 롤링 폴드가 한 체제만 덮으면 검증이 아니다. 무작위 셔플 대조·시장 표류·회전율 비용을 항상 같이 본다. 사전등록 홀드아웃이 이 오류를 잡았다 — 그것이 이 연구의 방법론적 기여.

### v1.7 (9/7 심야) — 사전등록: 긴 지평·횡단면 방향 탐색 (사장 지시 "변동성 탐지기로는 아쉽다, 가격 예측 이미지 파라미터를 다시 찾자")

- 가설 H4: 방향 예측 가능성은 절대 수익률 부호가 아니라 **횡단면 상대 수익**(판단 시각 유니버스 평균 대비)에, 그리고 **1~7일 지평**에 있다. 근거: 지평이 길수록 spread_z 가 커졌고(전수 지도), JKX 도 5~60일에서 방향을 찾았으며, 홀드아웃 실패의 원인이 시장 베타였다.
- 라벨: rel(상대 수익 kσ 3클래스) · relbin(상대 수익 부호). 지평: 1h 봉 24·72·168(1·3·7일), 4h 봉 6·18·42, 1d 봉 5·20. 창 60·120·240(1d 는 20·60). 격자 정렬(stride 1). 종목 = 폴드별 직전 12개월 상위 200.
- 순서: ① GBM 스크리닝(로컬, 4폴드) → 방향 지표 = 상대수익 spread_z, 십분위 적중률(hit_top·hit_bot, 기준 50%), 무작위 대조 ② 방향이 3폴드 이상 살아 있는 설정에만 이미지 CNN(흑백·5채널·캔들/라인) 을 pod 에 ③ 롱숏 시뮬(각 K/2, 비용 포함) ④ 홀드아웃은 **2차 사용**으로 명시하고 한 번.
- 채택 기준: 상대수익 spread_z ≥ 0.2 이고 hit_top·hit_bot 모두 > 52% 가 4폴드 중 3폴드 이상, 그리고 롱숏 시뮬이 4폴드 중 3폴드에서 무작위 초과.
- 실패 시: 방향은 이 데이터에서 예측 불가로 결론짓고 경보 머신으로 마무리.

## 10. 사장 결정 필요

1. 이 문서를 공개 리포에 지금 커밋할지. 커밋 해시가 사전등록의 시각 증거가 된다.
2. Runpod 예산 상한(제안 $250).
3. G1 메이저 10 목록 확정.
4. S7 LLM 모델: Qwen3.5-8B(A40 30시간) 기본, 4B 추가 여부.
5. S0 격자(R 6 × W 60/120/240 × H W/12·W/4·W/2)에 빼거나 넣을 수준이 있는지.
